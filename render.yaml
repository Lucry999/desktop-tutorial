services:
  - type: web
    name: clipforge-ai
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
      - key: RUNWAYML_API_SECRET
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
